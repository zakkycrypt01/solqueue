[workspace]
members = [
    "programs/*"
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = true
opt-level = "z"
codegen-units = 1
strip = "symbols"
panic = "abort"

[profile.dev]
overflow-checks = true
